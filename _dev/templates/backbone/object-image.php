<span class="sort-handle"><span class="screen-reader-text">Drag to sort</span></span><img src="<%= value && value[0] && value[0].sizes && value[0].sizes.thumbnail.url ? value[0].sizes.thumbnail.url : 'http://placehold.it/100x100' %>" class="image-preview" width="100" /><button class="select-image">select image</button> or <button class="add-from-url">Add from URL</button>: <input type="text" class="value" value="<%= 'string' === typeof value && value.length ? value : '' %>"<%= 'object' === typeof value ? 'disabled="disabled"' : '' %> /><span class="delete"><span class="screen-reader-text">delete</span></span>
