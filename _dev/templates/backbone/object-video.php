<h3 class="sort-handle">Video</h3>
<span class="video-preview"><%= value && value[0] && value[0].filename ? value[0].filename : 'Nothing Selected' %></span><button class="select-video">select video</button> or <button class="add-from-url">Add from URL</button>: <input type="text" class="value" value="<%= 'string' === typeof value && value.length ? value : '' %>"<%= 'object' === typeof value ? 'disabled="disabled"' : '' %> /> <span class="delete"><span class="screen-reader-text">delete</span></span>
